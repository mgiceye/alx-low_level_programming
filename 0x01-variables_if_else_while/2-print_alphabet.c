#include <stdio.h>

/**
*main- print alphabet
*Description: prints the alphabet in lowercase
*Return: 0
*/
int main()
{
        char ch;

        for (ch = 'a' ; ch <= 'z' ; ch++) {
        putchar(ch);
        }

        return(0);
}
