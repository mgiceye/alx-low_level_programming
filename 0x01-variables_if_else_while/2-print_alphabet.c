#include <stdio.h>

/**
*main- print alphabet
*Description: prints the alphabet in lowercase
*Return: 0
*/
int main(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
        putchar(ch \n);
}
return(0);
}
